[package]
name = "Rustmanager"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4"
mongodb = {version = "3.6.0"}
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }